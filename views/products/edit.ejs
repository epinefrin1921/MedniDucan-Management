<% layout('/layouts/boilerplate') %>

<div class="row">
    <h1 class="text-center">Uredi proizvod</h1>
    <div class="col-md-6 offset-md-3">
        <form action="/products/<%= product._id %>?_method=PUT" method="POST" novalidate class="validated-form">
            <div class="mb-3">
                <label class="form-label" for="naziv">Naziv</label>
                <input class="form-control" autofocus type="text" id="naziv" name="product[name]" required
                    placeholder="Unesi naziv" value='<%= product.name %>'>
                <div class="valid-feedback">
                    Izgleda dobro!
                </div>
            </div>
            <div class="mb-3">
                <label class="form-label" for="price">Cijena</label>
                <input placeholder="Unesi cijenu" class="form-control" type="text" id="price" name="product[price]"
                    required value='<%= product.price %>'>
                <div class="valid-feedback">
                    Izgleda dobro!
                </div>
            </div>
            <div class="mb-3">
                <button class="btn btn-success">Spremi proizvod</button>
            </div>
        </form>
        <a href="/products/<%= product._id %>">Vrati se na proizvode</a>
    </div>
</div>